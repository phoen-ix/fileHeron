SMTP-Test von {{ app_name }}.

Wenn du das liest, funktioniert deine SMTP-Konfiguration. Der Versand
hat mit den aktuell unter /admin/settings/email gespeicherten Werten
geklappt.

Gesendet am: {{ now|dt_locale(locale) }}
App-URL: {{ app_url }}

Diese Nachricht kannst du ignorieren — sie ist ein reiner Verbindungstest.
