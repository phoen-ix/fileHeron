Hi {{ display_name }},

Your {{ app_name }} account was just locked because there were too many failed login attempts. The lock expires automatically at {{ locked_until|dt_locale(locale) }}.

If this was you (e.g. a forgotten password), wait for the lock to expire or use the password reset link to set a new password:

{{ reset_url }}

If this wasn't you, someone is trying to guess your password. The lock has stopped them for now, but consider:
- Resetting your password to something unique you don't use anywhere else.
- Enabling two-factor authentication if you haven't already.

We won't send another email like this for the next 6 hours, even if more failed attempts happen.

Login source (rough): {{ ip_hint }}

- {{ app_name }}
