Hi {{ recipient_name|default("there") }},

A reminder: the share "{{ subject|default("(no subject)") }}" expires {{ expires_at|dt_locale(locale) }}.

After that, the files will be hard-deleted from {{ app_name }}.

Open: {{ share_url }}

- {{ app_name }}
